name: stress-check

on:
  push:
  pull_request:

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify scripts exist
        run: |
          test -d scripts
          test -f scripts/run-demo.sh
          echo "OK: stress-test scripts present"
