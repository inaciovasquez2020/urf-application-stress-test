name: stress-check

on:
  push:
  pull_request:

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify stress-test layout
        run: |
          test -d instances
          echo "OK: instances directory present"
